[project]
name = "music-context-player"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Matias Ceau", email = "matias@ceau.net" }
]
requires-python = ">=3.13"
dependencies = [
    "click>=8.2.1",
    "fastmcp>=2.10.6",
    "rich>=14.0.0",
    "streamlit>=1.47.0",
    "textual>=4.0.0",
]

[project.scripts]
music-context-player = "music_context_player:main"

[build-system]
requires = ["uv_build>=0.8.0,<0.9"]
build-backend = "uv_build"
